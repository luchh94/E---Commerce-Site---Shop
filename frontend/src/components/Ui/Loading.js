import React from "react";

import { Spinner, Container } from "./Loading.styles";

const Loading = () => {
  return (
    <Container>
      <Spinner />

    </Container>
  );
};

export default Loading;
